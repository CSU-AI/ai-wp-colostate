# Legacy content

These generated Markdown files contain readable copy extracted from the current live site. They retain the original URL and WordPress metadata in front matter.

Do not edit these files for the rebuild. Copy useful material into `content/planned/` so the historical snapshot remains intact.
